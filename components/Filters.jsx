import { View, StyleSheet } from "react-native";

export default function Filters() {
  return <View style={styles.containerFilters}></View>;
}

const styles = StyleSheet.create({
  containerFilters: {
    height: 80,
    backgroundColor: "#f5e5c9",
  },
});
